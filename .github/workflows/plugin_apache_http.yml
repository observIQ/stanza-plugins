name: plugin_apache_http
on:
  pull_request:
    paths:
      - ".github/workflows/plugin_apache_http.yml"
      - "plugins/apache_http.yaml"
  push:
    branches:
      - master
jobs:
  default_format:
    runs-on: ubuntu-latest
    steps:
    - name: Clone stanza-plugins
      uses: actions/checkout@v2

    - name: Clone Log Library
      uses: actions/checkout@v2
      with:
        repository: 'observIQ/log-library'
        path: log-library
        token: ${{ secrets.LOG_LIBRARY_ACCESS_TOKEN }}

    - name: Create Output Directory
      run: mkdir output

    - name: Run Stanza
      run: |
        docker run -d \
          --name stanza \
          --entrypoint /stanza_home/stanza \
          -v $(pwd)/log-library/cases/apache_http/default:/input \
          -v $(pwd)/output:/output \
          observiq/stanza:1.2.7 \
          --config /input/config/config.yaml

    - name: Pause 5 seconds
      run: sleep 5

    - name: Stop Stanza
      run: docker kill stanza

    - name: Check output
      run: |
        if sudo diff output/out log-library/cases/apache_http/default/expect/expect.json; then
          echo "pass"
        else
          echo "fail"
          exit 1
        fi

  observiq_format:
    runs-on: ubuntu-latest
    steps:
    - name: Clone stanza-plugins
      uses: actions/checkout@v2

    - name: Clone Log Library
      uses: actions/checkout@v2
      with:
        repository: 'observIQ/log-library'
        path: log-library
        token: ${{ secrets.LOG_LIBRARY_ACCESS_TOKEN }}

    - name: Create Output Directory
      run: mkdir output

    - name: Run Stanza
      run: |
        docker run -d \
          --name stanza \
          --entrypoint /stanza_home/stanza \
          -v $(pwd)/log-library/cases/apache_http/observiq:/input \
          -v $(pwd)/output:/output \
          observiq/stanza:1.2.7 \
          --config /input/config/config.yaml

    - name: Pause 5 seconds
      run: sleep 5

    - name: Stop Stanza
      run: docker kill stanza

    - name: Check output
      run: |
        if sudo diff output/out log-library/cases/apache_http/observiq/expect/expect.json; then
          echo "pass"
        else
          echo "fail"
          exit 1
        fi